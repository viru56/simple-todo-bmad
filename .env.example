# Use `db` when the server runs in Docker; use `localhost` when running server locally (e.g. npm run dev:server)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/todos
NODE_ENV=development
HOST=0.0.0.0
PORT=3001
CORS_ORIGIN=http://localhost:4200
LOG_LEVEL=info
